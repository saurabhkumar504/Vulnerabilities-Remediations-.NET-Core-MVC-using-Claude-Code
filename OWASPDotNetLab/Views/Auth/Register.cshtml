@{
    Layout = "~/Views/Shared/_Layout.cshtml";
    ViewData["Title"] = "Register";
}

<h2>Register</h2>
<p class="text-warning">
    Vulnerable: mass assignment - role and balance are accepted from the request body.
</p>

<form id="registerForm">
    <div class="mb-3">
        <label class="form-label">Username</label>
        <input class="form-control" name="username" />
    </div>
    <div class="mb-3">
        <label class="form-label">Password</label>
        <input class="form-control" name="password" />
    </div>
    <div class="mb-3">
        <label class="form-label">Email</label>
        <input class="form-control" name="email" />
    </div>
    <div class="mb-3 form-check">
        <input class="form-check-input" type="checkbox" id="exploit" />
        <label class="form-check-label" for="exploit">
            Send role=ADMIN and balance=999999 (mass-assignment demo)
        </label>
    </div>
    <button class="btn btn-primary" type="submit">Register</button>
</form>
<pre id="result" class="mt-3"></pre>

<script>
    document.getElementById('registerForm').addEventListener('submit', async function (e) {
        e.preventDefault();
        const fd = new FormData(e.target);
        const exploit = document.getElementById('exploit').checked;
        const payload = {
            username: fd.get('username'),
            password: fd.get('password'),
            email: fd.get('email')
        };
        if (exploit) {
            payload.role = 'ADMIN';
            payload.balance = 999999;
        }
        const res = await fetch('/Auth/Register', {
            method: 'POST',
            headers: { 'Content-Type': 'application/json' },
            body: JSON.stringify(payload)
        });
        document.getElementById('result').textContent = await res.text();
    });
</script>